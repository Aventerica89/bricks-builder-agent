{
  "name": "env-var-assistant",
  "version": "1.0.0",
  "description": "Chrome extension for managing API keys with 1Password",
  "private": true,
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:coverage": "node --test --experimental-test-coverage tests/*.test.mjs",
    "install-host": "cd native-host && ./install.sh"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
